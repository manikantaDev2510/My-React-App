import React from "react";
function prop(props){
    return(
        <div>
            {props.children}
        </div>
    )
}
export default prop;
// This is for to wi=rite code from the main index page in tags
// we use this rarely